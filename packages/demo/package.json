{
  "name": "@flarekit/demo",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "clean": "rm -rf dist",
    "test": "echo 'demo: no unit tests (verified via live run)'"
  },
  "dependencies": {
    "@flarekit/sdk": "workspace:*",
    "ethers": "^6.13.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
