{
  "name": "@flarekit/contracts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Solidity contracts that consume FDC proofs \u2014 FlarePayEscrow settles XRPL payments on Flare.",
  "scripts": {
    "build": "tsx scripts/compile.ts",
    "deploy": "tsx scripts/deploy.ts",
    "clean": "rm -rf out",
    "test": "echo 'contracts: verified by live Coston2 settlement (integration)'",
    "test:live": "tsx integration/settle-charge.ts"
  },
  "devDependencies": {
    "ethers": "^6.13.0",
    "solc": "^0.8.28",
    "tsx": "^4.7.0",
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0",
    "xrpl": "^5.0.0"
  },
  "dependencies": {
    "@flarekit/sdk": "workspace:*"
  }
}
